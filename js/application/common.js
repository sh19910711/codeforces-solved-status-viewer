(function() {
    'use strict';

    define(

        [
            'exports'
        ],

        function( exports ) {
            exports.DATABASE_SCHEME_VERSION = 0;
            exports.MANIFEST = {}; // parsed manifest.json data
        }

    )

}).call();
